plugins {
    id 'java'
}

group = 'nsu.syspro'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'

    implementation files("src/main/resources/SysPro.TM.JVM.jar")
}

test {
    useJUnitPlatform()
}